- 콘솔영역에서 컨트롤+L을 누르면 초기화 된다.

- 컨트롤 + 쉬프트 + c  = 주석 처리

- 범주형 자료 = 분류('클래스피 케이션')의 의미를 갖는 값들로 구성된 자료 , 산술연산을 적용할 수 없다.
 
- 수치형 자료 = 값들이 크기를 가지며 산술연산이 가능 (회귀 = regresion)

- 선형회귀 (수치형) , 단순선형회귀 , 다중 선형회귀

- sum  =  값들의 합

- mean = 평균

- median  = 중앙값

- max, min = 최댓값, 최소값

- var  =  분산

- sd  = 표준편차 

- sort = 정렬 ( 오름차순이 기본)

- range = 범위 ( 최솟값 , 최댓값)

- length = 개수( 벡터의 길이)

- 팩터
문자형 데이터가 저장되는 벡터의 일종, 저장되는 문자값들이 어떠한 종류를 나타내는 값일 때 사용


- 리스트
자료형이 다른 값들을 한곳에 저장하고 다룰 수 있도록 해주는 수단


- 매트릭스(matrix)와 데이터프레임은 2차원 자료를 저장하기위한 대표적인 자료구조


- 매트릭스와 데이터프레임 차이점
매트릭스에 저장되는 모든자료의 종류가 동일 , 데이터프레임에는 서로다른 종류의 데이터가 저장


- 테이블에서 가로줄 방향은 행 or 관측값이라고하고  , 세로줄 방향은 열 or 컬럼, 변수 라고한다.

-매트릭스  = 2차원 테이블 형태의 자료구조

- 1:20  = 매트릭스에 저장될 값을 지정

- nrow=4  : 매트릭스 행(row)의 개수를 의미

- ncol=5 : 매트릭스 열(column)의 개수를 의미


- 매트릭스 값의 추출

2차원 상에서 위치를 지정하려면 인덱스 2개가 필요

몇 번째 행, 몇 번째 열을 지정하는 2개의 인덱스 값이 필요

행(row)과 열(column)에 이름 붙이기


- 데이터 프레임 :  매트릭스와 마찬가지로 2차원 형태의 데이터를 저장하고 분석하는데 사용되는 자료구조 , 숫자형 자료와 문자형 자료가 결합되어 있는 형태

- head 함수는 자주 이용하는 함수 , tail 함수는 데이터셋의 끝부분에 있는 자료 중 일부를 보여주는 함수

- 행과 열의 방향 변환
t  =  4행 5열의 매트릭스를 5행 4열의 매트릭스로 변환(transpose)하는 경우에 사용하는 함수


- tips 데이터셋 불러오기 (library('reshape2')

- 화면에서 데이터 입력 받기 (library('svDialogs')

- 엑셀 파일 읽기와 쓰기 (library('xlsx')

- 다이아몬드 정보 제공 (library('ggplot2')

- 언어 발달 상황 진단 프로그램 (library('Stat2Data')

- 정책 지지도 그래프 그리기 (library('carData')

- 원그래프의 작성 (library('plotrix')

- 원그래프와 선그래프로 데이터 정리 (library('DAAG')


- as.numeric = 문자형의 숫자를 실제 계산 가능한 숫자로 변환하는 역할

- print =  하나의 값을 출력하며, 데이터프레임과 같은 2차원 자료구조 출력하고 출력 후 자동 줄바꿈

- cat = 여러개의 값을 연결해서 출력하며(벡터는 출력되나 2차원 자료구조는 출력되지 않는다.),
		 출력 후 줄바꿈을 하려면 '\n'이 필요


- 현재 작업 폴더 알기  = getwd()

- 작업 폴더 변경 = setwd()

- my.iris : 저장할 데이터가 들어 있는 자료구조 이름이 my.iris

• 'my_iris.csv' : 저장할 파일의 이름을 지정.

• row.names=F : my.iris를 저장할 때 행 번호/행 이름은 제외하라는 의미

- header=T : 파일의 첫 번째 행은 데이터의 값이 아닌 열 이름이라는 의미.

• sheetIndex=1 : 엑셀 파일의 첫 번째 시트를 읽으라는 의미.



실행결과를 파일로 출력하기

- sink('result.txt', append=T) # 파일로 출력 시작


- sink( ) # 파일로 출력 정지















